---
title: '第 12 章：写 Go 后端的几个长期习惯'
volume: 1
chapter: 12
description: '总结上册：简单性、错误边界、接口克制、测试优先、context 传递和服务可运行性。'
---

> 本章问题：学完上册后，哪些习惯比记住更多语法更重要？

---

## 简单不是少写几行

Go 经常被说成简单语言。但这里的“简单”不是指代码一定最短。

显式错误处理会多写几行。结构体字段要大写才能导出，JSON tag 又要写一遍。没有异常，没有继承，没有太多语法糖。刚开始看，这些都不像“简单”。

Go 的简单更接近另一件事：读代码时，你不需要猜太多。

函数返回了 `error`，你知道它可能失败。包里首字母大写的名字，你知道外部可以访问。handler 里写了状态码，你知道接口如何响应。`context` 一路传下去，你知道请求取消有机会传到底层。

后端服务会长期运行，也会长期被修改。能被读懂，比一时写得短更重要。

---

## 错误路径要可见

Go 后端代码里，最值得保留的习惯是认真处理错误。

不要忽略错误：

```go
data, _ := os.ReadFile(path)
```

除非你非常确定错误无关紧要，否则不要这样写。

也不要在底层到处 `log.Fatal`。底层函数应该返回错误，让上层决定如何处理。入口层、HTTP handler、后台任务边界，才是更适合记录和翻译错误的地方。

一个好的错误链条应该能回答：

- 哪一步失败？
- 处理的对象是什么？
- 底层错误是什么？
- 对用户应该返回什么？

这比“优雅”的异常包装更实用。

---

## 接口要小，出现要晚

Go 的 interface 很轻，所以也很容易被滥用。

不要一开始就为每个 service、repository、manager 都写一个接口。接口应该来自真实需求：

- 调用方只需要某个最小能力
- 测试需要替换外部依赖
- 你确实有多个实现
- 你希望隐藏一个包的内部细节

接口越小，越有用。一个只有 `Send(...) error` 的接口，往往比一个包含十几个方法的 `UserService` 更容易维护。

先写具体代码，再在摩擦出现时提取接口。这是 Go 里很实用的节奏。

---

## 测试先覆盖稳定规则

测试不是为了证明你很专业，而是为了让你敢改。

优先测试那些稳定、明确、容易出错的规则：

- 参数校验
- 分页计算
- JSON 解析
- 配置默认值
- 错误分支
- HTTP 状态码

不要一开始就把测试写成复杂工程。一个表格驱动测试，一个 `httptest` handler 测试，就能给小项目带来很大安全感。

当业务变复杂，测试也会自然变复杂。上册阶段先把最朴素的测试习惯建立起来。

---

## context 是后端函数的边界信号

看到一个函数需要访问数据库、调用外部接口、等待队列或执行耗时任务时，优先考虑让它接收 `context.Context`：

```go
func FindUser(ctx context.Context, id int64) (User, error)
```

这不是形式主义。它表示这个函数属于某个请求或任务的生命周期，可能会被取消，也可能有超时。

不要把 context 存进结构体字段里长期保存。通常应该把它作为函数第一个参数，一层层传下去。

---

## main 函数是服务的装配点

`main` 不应该塞满业务逻辑。它更像装配点：

- 加载配置
- 初始化依赖
- 注册路由
- 创建 server
- 启动服务
- 处理退出

这样做的好处是，业务函数可以脱离进程启动逻辑单独测试。你不需要启动整个服务，才能测试一个用户校验函数。

---

## 上册到这里完成了什么

如果你跟着上册敲过代码，现在应该已经见过 Go 后端的基本路径：

```text
模块初始化
  ↓
基础类型和函数
  ↓
结构体、方法、接口
  ↓
错误处理
  ↓
包和 internal
  ↓
配置读取
  ↓
HTTP handler
  ↓
测试
  ↓
goroutine、channel、context
  ↓
构建和运行
```

这条路径还没有进入数据库、缓存、消息队列、认证授权和可观测性。但它已经足够支撑一个小型服务的骨架。

接下来进入中册或下册时，我们可以把这些基础放到更真实的工程场景里：连接数据库，设计 API，做认证，拆分包边界，处理并发任务，加入日志、指标和追踪。

---

## 最后一条建议

写 Go 时，不要急着证明自己会很多抽象。

先让代码能跑，再让失败路径清楚，再让边界变小，再让测试补上。很多时候，这比一开始设计一个漂亮架构更可靠。

Go 的价值，也常常就在这里：它把你拉回到程序最基本的事情上。
